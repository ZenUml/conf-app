.env
.env.mixpanel*
.env.forge
.env.forge.local
.pluprc
ui-debug.log
.DS_Store
node_modules
.firebase
firebase-debug.log
tests/e2e-tests/screenshots

dist
storybook-static
.cache
.wrangler
wrangler.toml
log.txt

.tmp/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*

# coverage
coverage
# Sentry Config File
.env.sentry-build-plugin

wrangler.toml
!workers/*/wrangler.toml
.playwright-*
playwright-report/
test-results/
*.png
!public/image/*.png
!public/drawio/**/*.png
# Documentation screenshots referenced from PRs and docs pages. Narrow on
# purpose, same as the two above: *.png stays ignored everywhere else so
# stray captures and test artifacts cannot be committed by accident.
!docs/images/*.png
.harness/
.worktrees/
.worktree/
.claude/worktrees/
.mcp.json

# Local runtime / agent cruft
.claude/scheduled_tasks.lock
.claude/skills/**/__pycache__/
.claude/projects/
.codex/
.superpowers/

# Generated release-notes drafts from /release-app runs (published to GitHub releases)
/release-notes-*.md

# Local Mixpanel exports — raw production telemetry, never commit
mixpanel-export*

# Sensitive files — never commit
credentials.json
.dev.vars
scripts/dev-config/
scripts/confluence_domain_migration/
.worktrees

# Personal spotcheck / debugging snapshots — never commit (may contain customer names)
/page-snapshot.yml
/paywall-snap-*.yml
/spotcheck-*.yml

# One-off E2E spike specs — throwaway investigation scripts, not a maintained
# suite. Anything worth keeping gets promoted into tests/e2e-tests/tests/.
tests/e2e-tests/tests/spike/
tests/e2e-tests/playwright.spike.config.ts

# AsyncAPI Studio build output — rebuilt from vendor/asyncapi-studio submodule
# by scripts/build-studio.sh (called from pnpm build:asyncapi). Mirrors
# AsyncAPI-Conf-V2's setup: build fresh, do not commit.
static/asyncapi-studio/


# scratch (deeplink staging validation: secret, mint scripts, PNG)
tmp/

# pnpm store
.pnpm-store

output/playwright

# Generated D1 cache: contains real tenant installation history.
/.claude/skills/new-customers/scripts/d1_first_seen.json
.container-service-assets/local-account

Profile 8