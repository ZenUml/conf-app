.env
.env.mixpanel*
.env.forge
.env.forge.local
.pluprc
ui-debug.log
.DS_Store
node_modules
.firebase
firebase-debug.log
tests/e2e-tests/screenshots

dist
.cache
.wrangler
wrangler.toml
log.txt

.tmp/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*

# coverage
coverage
# Sentry Config File
.env.sentry-build-plugin

wrangler.toml
!workers/*/wrangler.toml
.playwright-mcp/
playwright-report/
test-results/
*.png
!public/image/*.png
!public/drawio/**/*.png
.harness/
.worktrees/
.worktree/
.claude/worktrees/
.mcp.json

# Local runtime / agent cruft
.claude/scheduled_tasks.lock
.claude/skills/**/__pycache__/
.claude/projects/
.codex/
.superpowers/

# Generated release-notes drafts from /release-app runs (published to GitHub releases)
/release-notes-*.md

# Generated cross-platform links mirroring .claude/skills (run `pnpm link:skills`)
/.agents/skills/*
!/.agents/skills/.gitkeep

# Local Mixpanel exports — raw production telemetry, never commit
mixpanel-export*

# Sensitive files — never commit
credentials.json
.dev.vars
scripts/dev-config/
scripts/confluence_domain_migration/
.worktrees

# Personal spotcheck / debugging snapshots — never commit (may contain customer names)
/page-snapshot.yml
/paywall-snap-*.yml
/spotcheck-*.yml

# AsyncAPI Studio build output — rebuilt from vendor/asyncapi-studio submodule
# by scripts/build-studio.sh (called from pnpm build:asyncapi). Mirrors
# AsyncAPI-Conf-V2's setup: build fresh, do not commit.
static/asyncapi-studio/
