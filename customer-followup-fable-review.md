# 客户工作流 skills：与 Fable 的共同方案

2026-09-12，完成四轮 Fable 评审与讨论，返回记录均确认由 Fable 5.1 提供主要评审。**目前没有需要用户裁决的实质分歧。** 原始回复按轮次保存在后面，早期已撤回意见不代表最终方案。

本轮仅讨论设计与审计结果：Fable 未独立读取源码或生产数据，工具在评审调用中禁用；本地审计及匿名复现由主任务完成。评审当时尚未实施；随后经用户授权，第一版三个 skill、共享规则和表格适配已落地。以下原始评审保留为历史，不代表当前实施状态。

## 共同结构

| 入口 | 负责什么 | 边界 |
|---|---|---|
| `customer-followup` | 按需取证，判断转化/留存/挽回/暂缓，准备并执行已有授权内的动作，核验后写回 | 联系策略与动作决策集中在这里；逐家审阅不重复审批已授权的事 |
| `customer-lifecycle` | 独立回答最近安装、卸载、许可或订阅变化、到期等状态问题，也供主流程使用 | 薄的只读入口；复用现有查询及同一份信号矩阵，不夹带联系建议；未覆盖或不可推导明确标出 |
| `customer-feedback` | 独立查询并归类 Marketplace、Mixpanel、D1、邮件与工单反馈 | 保留原始诉求、出处、窗口、处理状态和关联证据；不自动决定联系，不把行为事件直接当投诉 |

共同使用产品目录、站点/产品/许可身份规则、角色证据、信号与来源矩阵以及单一定价实现。`tenant` 等现有能力复用并收窄，不把所有查询重写一遍。联系策略由主流程维护。

## 讨论中改变的意见

- Fable 第一轮主张只建两个 skill；补充用户已经单独筛查安装/卸载/到期这一使用场景后，他接受生命周期的独立薄入口。它的价值是可单独完成任务，不是制造另一套查询实现。
- Fable 接受：同一 agent 加载 Markdown skill 不会剥夺其他工具权限。只读是流程职责约定，第一版不为此增加执行框架；不能宣称已做到技术隔离。
- Fable 接受：授权不能按“同类收件人”推扩。一次性批准给一位管理员发信，不代表批准联系所有管理员。
- Fable 接受：沟通、机会阶段、许可付款和暂缓条件分开记录，不能把付款放进单一邮件状态链。
- 七天可以作为反馈快捷默认，复盘时可改变窗口。跨来源归并要有核实过的关联依据；近似时间只标疑似，不强行合并。
- 生命周期可直接复用只读底层查询，不必等所有旧 skill 改写完成；但不能加载旧收入筛查中夹带的联系策略，再称输出只读。该旧策略仍需要单独清理。

## 授权与结果记录

授权按用户实际表达的范围执行。一次性批次不自行加入后来的候选；持续或动态范围必须有用户明确授权，不能从一次性授权推导。缺少具体执行必需的信息时只补齐该信息，不重复审批、不强制要求预定截止日，也不擅自转换授权类型。具体外发须能对应实际授权对象、内容和渠道；调度只在用户授权范围内设定。

记录分开保留：机会阶段；每个沟通动作的草稿、定时、发送、回复及证据；许可与付款事实；暂缓原因和恢复条件。沿用表格字段与日志，不为此建立新的事件系统。Local CRM 模拟结果不写成实际执行结果。

暂缓客户由后续运行主流程时复核恢复条件；持续后台监控和自动外发均不因这个设计而获得授权。

## 先修什么

先解决会误导事实的缺陷：授权查询失败被当作无授权、用户授权键解析成错误空间、未知身份计为真实人数；同时修正定价口径与缺失安装查询。随后整理产品覆盖、过时统计说明、散落的联系策略和 Codex 入口。查询失败与缺少覆盖不能再输出为“没有”。

第一版按主流程、反馈和生命周期的实际需求逐步接通；新入口不能掩盖底层来源尚不可用。每条结果携带来源、发生/查询时间、适用范围与未核实项。

## 相关材料

- [更新后的结构图](customer-followup-skill-relationships.md)
- [现有 skills 审计与匿名复现](customer-followup-skill-audit.md)

## 逐轮评审记录

以下为各轮原始回复，仅供追溯；最终结论以上文和第四轮澄清为准。

### 第 1 轮

以下评审仅基于你提供的设计全文与审计摘要，我没有独立核验源码、生产表或客户数据。

**A. 三个新 skill：保留两个，反对一个**

- `customer-followup` 合适。它是唯一决定与执行的地方。内部要拆成三种输出：决定，包含转化、留存、挽回、暂缓及恢复条件；动作，须落在既有授权范围内；记录，即状态写回。同一 skill，分开参考文件。
- `customer-feedback` 合适。现有能力没有一处同时读 D1 FeedbackReport、Mixpanel 满意度、Marketplace 取消文字和邮件工单。但“默认近 7 天”与它同时声称的产品改进、问题复盘用途冲突，时间窗应是参数而非身份。
- `customer-lifecycle` 我不同意作为独立 skill。它只是包装 `new-customers`、`forge-installs`、`marketplace`、`income-radar` 加 Local CRM 分类。真正有价值的内容是三样：信号乘来源乘可推导性的矩阵、到期分类表、证据规则。这三样都是参考文件，不是运行能力。而且包装 `income-radar` 会连带继承它夹带的联系策略。建议先降为主 skill 的 reference，只有出现 followup 之外的第二个调用方时再升级。
- 漏项，图中无人负责：跨 Marketplace、Forge、D1、Mixpanel、Gmail/JSM 的联系人身份与职责解析；统一产品清单，现在是四个、三个、含 Mini Sites 五个三种口径；暂缓客户恢复条件满足时由谁重新筛查。

**B. 保持、收窄、弃用**

- 原样保持：`mixpanel`、`macro-count`、`conf-app`、`search-conversation-history`、`1by1` 仅作审阅节奏、Local CRM 作只读分类来源。
- 收窄：`new-customers` 输出候选并带 D1 历史状态字段，不再说“新客户”；`client-health` 标明只是 Lite 使用相对排序，并输出“同站点已有 Full”标志；`income-radar` 与 `tenant` 变为纯事实查询，删除策略文字；`extend-space-license` 只做延期，弃用内部草稿脚本和代理商推 299 规则；`forge-installs` 修复或删除 check.py 引用并对齐产品清单。
- 共用脚本，只留一份实现：年度席位定价，废除年费十倍月费文字和延期工具里的重复算法；产品清单；联系人职责解析。
- 主 skill reference：分析口径、联系人策略、记录状态机、暂缓与恢复条件规范、生命周期信号矩阵。
- Codex 入口：为 `client-health`、`marketplace`、`extend-space-license` 补链接，沿用现有链接模式，不复制。

**C. 职责边界要靠结构，不靠一句政策**

- 查询 skill 返回事实，每条带来源、时间、覆盖窗口和可推导或未核实标记。输出结构里没有“建议动作”字段，也不持有发送和定时工具。这才是查询者无法擅自外发的保证。
- 身份与许可匹配由 `tenant` 加共用职责解析脚本负责。查询方只报告找到的角色，不从“技术联系人”推断管理员。
- 决定与动作只在 `customer-followup`。授权范围按动作类型乘收件人类别乘渠道记录一次。每个动作引用所属范围，只有落在所有范围之外才提示审批。这样 `1by1` 变成逐家审阅、批量按范围执行、只对例外提问。
- 写回只有 `customer-followup` 一个写入方。状态是枚举字段，草稿、定时、已发、已回复、已付款，每次迁移引用证据，如邮件 ID 或付款记录。Local CRM 模拟动作永远不写成真实结果。

**D. 先修后建**

修复顺序：定价单一实现；产品清单，明确 Mini Sites 是否纳入；缺失的 check.py；剥离 `tenant`、`income-radar`、`extend-space-license` 的策略文字；`new-customers` 候选状态；Mixpanel、conf-app、macro-count 过时描述；Codex 链接。然后建 `customer-followup` 及其 reference，再建 `customer-feedback`，生命周期矩阵只作参考文件。

检验场景：

1. 站点管理员，Lite 试用 5 天后到期，同站点已有 Full。预期：不提 299，不因到期本身联系，client-health 必须显示 Full 标志，倾向暂缓并写恢复条件。
2. 新 Full 许可，唯一联系人是技术联系人，无 D1 历史。预期：记为候选而非新客户，职责未定前不选择话术。
3. Forge 差异显示卸载，Marketplace 仍有效，D1 三天前有一条故障反馈，Mixpanel 有对应成功事件。预期：反馈返回一条记录两个来源，归为挽回，卸载日期只写快照区间。
4. 代理商的空间负责人通过工单申请延期。预期：`extend-space-license` 只延期，是否提 299 按职责不按代理商，到期记为用户空间临时授权而非 Marketplace 到期。
5. 客户两周前回复“Q4 预算后再谈”，`income-radar` 现在标记续费机会。预期：暂缓并写带日期的恢复条件，状态保持已回复，不重发。
6. 自动续费客户临近维护期到期，Marketplace 续费状态标为不可推导。预期：报告未知而非已续费，无计费失败或受阻事件等逐案证据不联系。
7. 八个候选属同一已授权消息类型，其中一位是采购负责人，属新收件人类别。预期：七个审阅后直接执行，只对第八个提问。

### 第 2 轮

以下回应仍只依据你提供的设计文本、审计摘要和你自述的复现结果，我没有独立核验源码、生产数据或脚本行为。

**1. 生命周期薄入口：接受，附条件**

你的反驳成立。“最近客户有什么变化”是用户直接运行的独立任务，这本身就是 followup 之外的第二个调用方。reference 文件不能运行，让用户为了状态筛查进入 followup 会被迫加载决定逻辑，手工调四个来源又会每次重做聚合。这就是薄入口相对 reference 的独立价值。

条件如下，缺一条我会回到反对：
- 与 followup 共用同一份信号乘来源乘可推导性矩阵和同一份产品清单，不各自维护。
- 输出格式就是 followup 消费的记录格式，没有建议、推荐、下一步字段。
- 不能查的信号明确输出未知，不从当前状态虚构变化日期。
- 在 income-radar 剥离策略文字之前不包装它。这是顺序要求，不是分歧。

**2. 工具隔离：撤回前一轮说法**

我前一轮说“不持有发送和定时工具就能保证不擅自外发”是错的。同一 agent 上的 Markdown skill 不会剥夺 agent 的其他工具。同意删掉这句。替代表述是：查询 skill 的输出结构里没有动作字段，发送只作为 followup 中显式命名的一步存在，第一版查询流程无写入步骤。真正的权限隔离等独立执行器出现后用白名单强制。

**3. 授权：接受你的模型**

按角色类别推扩是我的设计缺陷。批准联系一位管理员不等于批准联系所有管理员。记录用户实际授权的收件人或明确批次、渠道、内容或模板范围、发送或定时方式和时间约束，在此范围内连续执行不再确认。

补一条收紧：若用户按规则授权一个批次，批次成员在授权时刻冻结，之后新出现的候选不自动加入，需另行授权。

前一轮场景 7 相应改写：七个候选直接执行，是因为它们在用户明确授权的批次内，不是因为同属一个类别。第八位采购负责人不在批次内才提问。

**4. 状态：接受**

付款与沟通正交，单一枚举链是错的。分别保存机会阶段、每个沟通动作的状态及证据标识、许可与付款事实、暂缓与恢复条件，按实际系统证据更新。用现有表格字段和日志，不建事件溯源。

**5. 反馈窗口与归并：这是一致，不是分歧**

前一轮我已说时间窗应是参数而非身份，你的“7 天为快捷默认、复盘可用 30 或 90 天”与此相同。关联条件也接受：只有核实过的共享引用才能把 D1 报告和 Mixpanel 事件归为一条；近似时间只标疑似关联。查询失败或未覆盖输出为未知，不是没有反馈。

前一轮场景 3 相应改写：若 Mixpanel 事件不携带报告引用，预期输出是一条 D1 反馈记录加一条疑似关联的 Mixpanel 事件，两者各带出处，不合并为“一条两个来源”。

**6. 脚本缺陷：接受，按你的复现结果**

我无法自行验证这三项，按你的描述采信。三项都直接影响许可和人数判断，应与定价、缺失的 check.py 一起进入第一批修复。其中 KV 命令失败返回空并当作无授权应排第一，因为它把错误变成了否定性许可事实，正面违反整个设计依赖的“未知不等于没有”规则。用户级键误解析为空间会污染 tenant 画像和延期对象，排第二。健康评分把未知账号计为创建者会抬高 Lite 排序，排第三。

**两点澄清：均接受**

保留某 skill 只指入口职责，过时的 Forge、D1、Graph 文案必须改。暂缓恢复条件由 followup 每次运行时评估，持续监控需另有用户调度授权。

**共同建议结构**

- 三个新 skill：customer-followup 负责决定、动作、写回；customer-lifecycle 和 customer-feedback 为只读查询入口，输出无动作字段。
- 共用资产只留一份：信号矩阵、产品清单、年度席位定价、联系人职责解析。
- 授权记录以用户实际给出的收件人或冻结批次为准，不按角色推扩。
- 状态分四类字段保存，每次变更引用证据。
- 修复顺序：KV 失败返回空、用户级键解析、健康评分未知账号、定价单一实现、check.py、策略文字剥离、过时文案、Codex 链接。之后建 followup，再建 feedback 和 lifecycle。

**仍有分歧**

实质分歧已基本消除。只有两处待你确认，若你不接受则保留为分歧：
- 第 3 项的批次冻结规则。
- 第 1 项中 lifecycle 在 income-radar 剥离策略前不包装它的顺序要求。

### 第 3 轮

以下回应仍只依据你提供的文本，我未独立核验代码或脚本行为。

**A. 批次冻结：接受，且可以精确限定为一次性批次。**
冻结规则从一开始只约束“从一次性授权能推出什么”，不否定用户明确给出的持续授权。我第二轮已说持续监控需用户另行调度授权，你的补充与此一致，所以这是澄清而非让步。边界按你说的定：动态授权必须由用户明确给出，并带筛选条件、内容或模板、渠道、频率和时间上限。缺任一项或措辞含糊，就按一次性批次冻结处理，新候选另行请示。不从一次性批次推断动态授权，也不因此创建自动化。

**B. income-radar 顺序：接受。**
我的条件本质是“lifecycle 不能暗中继承 radar 的联系建议”。你给的两条路径都满足：要么先剥离 radar 的策略文字，要么 lifecycle 直接复用已只读的底层查询函数，完全不加载 radar skill。因此修复顺序相应调整：策略文字剥离仍在修复清单内，因为 radar 仍是用户会直接运行的 skill；但 lifecycle 走复用路径时不再被它阻塞。底层结果必须经范围、新鲜度、未知状态校验后才能进入 lifecycle 输出。同意不因此暂停与 radar 无关的安装、许可查询，不新增执行器，不重复写 connector。

**最终结论：不再有实质分歧。**
其余共同结构与你列出的一致，不再重复。最重要的三条落实条件：

1. KV 命令失败返回空并被当作无授权的缺陷第一个修，因为“未知不等于没有”是整个设计依赖的规则，其他项都建在它之上。
2. 信号矩阵、产品清单、年度席位定价、联系人职责解析各只保留一份，三个 skill 共用；lifecycle 与 feedback 的输出结构里没有任何建议或动作字段。
3. 授权记录只记用户实际给出的内容：具名收件人、冻结批次，或带完整边界的明确动态范围。不按角色推扩，不按时间接近归并反馈，不合并未核实共享引用的记录。

### 第 4 轮

接受你提出的共同表述，不再坚持原限制。

我撤回第三轮新增的“动态授权必须带频率和时间上限，缺任一项即按一次性冻结”。理由是：“每周按已审模板发给符合条件的新客户，直到我叫停”本身就是有边界的明确持续授权，边界是用户的叫停而非预定截止日。把缺项转换为一次性授权，已超出“不从一次性授权推导持续范围”这条规则本身。

共同表述保留了我真正关心的两点：持续范围不能由一次性批次推导；每次具体外发都能对应实际授权的对象、内容和渠道。读取与整理不因缺外发参数停下，缺执行信息只补齐该项，这些我同意。

第三轮其余实施条件不变。本判断仅依据你提供的文本，未独立核验生产数据。
