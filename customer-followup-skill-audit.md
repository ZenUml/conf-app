# 客户筛选与跟进 skills：结构与现状审计

审阅日期：2026-09-12。范围为本次客户工作流涉及的现有 skills、直接引用的脚本及相关产品代码。下列结论来自本地文件与匿名的最小复现；没有在本轮重新核验生产许可证、价格、安装、部署或客户反馈。这是实施前的审计快照。随后已完成关键修复与三个入口；历史问题描述保留供追溯。当前覆盖边界见新 skills 的共享证据规则。

## 优先修正：会改变客户判断或执行结果

| 问题 | 已核实的依据 | 影响与处理方向 |
|---|---|---|
| 年费口径互相冲突 | `.claude/skills/marketplace/SKILL.md:44` 写年费为月费十倍；`mp_pricing.py` 的 `quote` 使用发布的年度席位档，`tiers` 命令仍实际输出旧公式。延期工具另有同类定价函数。 | 报价必须统一到一个共用实现；说明、报价与档位展示采用同一口径。旧的约 68 席位盈亏比较不能继续硬编码。这里没有重新确认当前公开价格。 |
| 安装查询命令无法运行 | `.claude/skills/forge-installs/SKILL.md:42` 引用的 `scripts/check.py` 不存在；运行帮助命令得到文件不存在。 | 先恢复可用安装查询，再承诺 lifecycle 能发现安装/卸载。保留快照差分、缺少基线不等于零变化的原则。 |
| 授权查询失败被当成无授权 | `marketplace/scripts/mp_report.py:550` 未检查命令返回码；匿名模拟失败且标准输出为空时，`_kv_space_licenses` 返回 `[]`。 | 区分成功查无记录、查询失败、未查询；失败时不可据此判定客户没有授权。 |
| 用户授权键被当成空间键 | 同脚本用键的最后一段作为空间。输入匿名键 `license:example-cloud:ENG:example-user`，输出空间为 `example-user`。延期脚本已支持用户级授权。 | 按明确键结构解析空间和用户范围，兼容空间级授权；无法解析时保留原始证据并标待核验。 |
| 联系策略仍按代理商决定是否展示单空间 | `extend-space-license/scripts/outbound_contact.py:301` 有代理商只推 Full，无代理商则加入单空间方案。 | 与本次确定的“管理员/统一授权负责人只推 Full”不一致。联系人职责与采购渠道分别核实，单空间策略不能仅由代理商字段决定。 |
| 查询 skill 混入行动政策 | `tenant/SKILL.md:84` 把产品内手段写成始终允许并要求先做，另带外联限制；`income-radar/SKILL.md:44` 把试用到期称为应提前行动的转化窗口。 | 查询返回事实、风险和缺口；联系、延期和暂缓决策集中到主流程。任何读数都不自行创造操作授权。Full 到期本身不足以触发催购。 |
| 匿名占位身份被计为真实创建者 | `client-health/scripts/health_score.py:213` 仅以非空判断身份。匿名复现中，只有 `unknown_user_account_id` 的输入仍得到一个创建者。 | 共用身份排除规则；不能把共享占位符算作客户人数。历史匿名覆盖还需要结合当前事件结构与窗口说明。 |

## 过时描述与覆盖限制

1. **Forge 与 D1 关联说明落后于实现。** 全局 `conf-app/SKILL.md:31,73,87` 和 `macro-count/SKILL.md:31,42,110` 仍说 Forge 数据无法关联站点。当前 `functions/forge-custom-content.ts:127–168` 已写入 `CustomContent.cloudId`、更新 `ForgeInstallation.clientDomain` 并维护站点映射；迁移 0022 增加了相应字段。需要检查生产迁移、空值和历史覆盖后升级查询，不能从代码存在直接推导所有历史记录可关联。宏计数脚本目前仍只有旧的 Connect 关联路径。
2. **Graph 查看事件缺失的描述已经过时。** `conf-app/SKILL.md:110`、`macro-count/SKILL.md:32,151,167` 仍称 Graph 没有查看事件。当前 `src/components/Viewer/ForgeGraphViewer.vue:155` 调用 `trackRenderTime('graph', …)`，后者发出 `macro_viewed`；`mixpanel/SKILL.md:134` 也已经记载此修复。需要保留历史时间分界，不能一概补算旧事件。
3. **D1 不应被描述为完整的宏保存台账。** 全局 `conf-app/SKILL.md:30` 称每次创建/编辑均有持久记录，但当前同步端点在内容拉取失败时返回 `synced: false`。Confluence 是内容权威来源；D1 只能在核实覆盖后用于统计交叉检查，不能保证必然大于当前存量。
4. **身份追踪文档有过期的待上线状态。** `mixpanel/SKILL.md:49–50` 仍把匿名身份改动描述为待合并，当前 `trackAnalyticsEvent.ts` 已含账号缓存和匿名身份处理。旧窗口的匿名比例是历史测量，不是当前全量保证。
5. **新增客户的判断过强。** `new-customers/scripts/new_customers.py:77–89` 用许可周期起始时间和 D1 历史判断，缺少 D1 记录便标 `NEW / forge-direct`。缺少历史不等于确认新安装；同一公司新产品、迁移、续订周期均需区分。应输出新增许可候选、已知历史和信心，而不是直接声称真实获客。
6. **产品全集没有统一。** `forge-installs` 的 all 指三个产品，`new-customers` 指四个，Marketplace 的 vendor-wide all 范围又不同。当前业务还涉及 Mini Sites。需要统一产品目录，并在输出明确纳入/未覆盖的产品。
7. **健康评分有明确适用范围。** `client-health` 只覆盖窗口内有查看记录的 Lite。创建者人数/席位不等于全员使用覆盖；查看趋势也不代表编辑趋势。新客户前一窗口为零会使增长显得很高；同时装 Full 的客户需要额外核验。分数是群体内相对排序，不是转化概率。
8. **缓存和查询缺口必须随结果输出。** 本地 Marketplace 快照、D1 缓存和 Mixpanel 窗口并不天然同时更新。至少保留来源、发生时间、查询时间、覆盖窗口、成功/失败/未查与缺口；不能把某一来源缺失说成没有行为或反馈。
9. **Local CRM 是分类与证据要求的参考，不是完整实时数据源。** 其部分到期事件来自用户/空间授权；Marketplace 到期、取消等仍有不可推导或未核实的类别，界面动作是本地模拟。不能直接把模拟成功写成客户动作已执行。
10. **D1 功能反馈不能遗漏或与满意度混合。** 相邻开发目录有独立 `FeedbackReport` 实现，正文与可选截图在 D1，Mixpanel 记录提交流程。当前生产可用性与覆盖尚未核验；流程事件不能再算一份用户意见，打开工单也不等于已提交工单。

## 应保留的复用与需要集中维护的内容

| 能力关系 | 建议 |
|---|---|
| `marketplace`、`tenant`、`income-radar` 已共用 Marketplace 脚本 | 保留不同问题的入口。通用数据查询、单站点画像、续费候选筛选有不同用途；收回散落其中的联系政策。 |
| `mixpanel` 与 `conf-app` | 前者维护事件/身份/查询规范，后者解释跨来源业务含义。事实口径只有一处维护，其他引用。 |
| `macro-count` 与活动分析 | 保留存量查询专门能力；空间当前宏数量、窗口内创建次数、查看过的宏实例数不可互换。 |
| `client-health` | 保留 Lite 相对排序用途，标清适用人群与缺口；主流程不把评分当最终联系决定。 |
| `extend-space-license` | 保留有明确范围的授权操作；通用销售文案和联系人策略移交主流程。 |
| `1by1` | 保留逐家审阅节奏，去掉对已授权连续整理的重复审批；具体外发仍须存在对应授权。 |
| `search-conversation-history` | 查过去决策和承诺；当前邮件发送、付款、许可等依旧回原系统核实。 |
| 新增 `customer-lifecycle` 与既有查询 | 组合现有来源与变化证据，不再实现一份许可、安装或收入查询；来源缺口公开表达。 |
| 新增 `customer-feedback` | 可以独立用于产品研究。负责归类原始意见与来源关系，不替主流程决定外联。 |

应集中维护：产品目录、站点/产品/联系人身份匹配、时间与缓存口径、未知身份与内部站点排除、定价实现、联系策略、动作授权与实际结果记录。是否需要独立 skill，取决于是否有独立用户任务；共用函数和参考文件本身不必再包装一个 skill。

## 入口并非重复文件

本地解析确认，大多数 `.codex/skills/…` 是指向 `.claude/skills/…` 的目录链接，全局 `conf-app` 和历史查询亦如此，不能按两份内容进行合并。真正的发现问题是项目中 `client-health`、`marketplace`、`extend-space-license` 缺少对应 Codex 入口；实现和名称要先明确，再修复入口。

## 核验范围与下一步

- 已执行缺失安装脚本的命令检查与旧定价档位输出检查；已用匿名输入复现授权查询失败伪装为空、用户授权键空间解析错误、占位身份计数三个问题。
- 未刷新生产客户数据、未核验当前年度价格、未验证相邻目录功能反馈的部署；没有联系客户或修改任何现有 skill。
- 建议先修会误导事实与权限的缺陷，再统一共享规则，然后以少量匿名场景试用主流程。Fable 的结构评审另行记录，不把模型评审视为生产事实验证。

## 实施进展（2026-09-12）

已修复 KV 失败/用户键、占位账号、共享定价、缺失安装助手、候选分类，收窄联系政策并停用旧外联入口；更新相关文档和 Codex 链接。Sheets 改为独立许可/付款/沟通/暂停字段及逐条记录。65项离线测试通过。生产 D1 功能反馈、Mini Sites 独立安装身份以及 Forge 历史宏数据覆盖仍需逐次核实；没有把来源缺口当作已完成的生产验证。
