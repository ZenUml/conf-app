node_modules/
dist/
.vite/
screenshots/
.local/

# Real extraction (tenant hostnames, cloud IDs, contact identities).
# docs/policies/client-privacy.md keeps this out of the public repo.
# Generate with: pnpm --dir local-crm run … (see README.md § Real data)
src/data/local/
